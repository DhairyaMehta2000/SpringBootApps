package state;

import entities.ATM;
import entities.User;

public interface ATMState {

	void insertCard(ATM atm, User user);

	void validateCard(ATM atm, User user);

	void selectOperation(ATM atm, User user);

	void ejectCard(ATM atm, User user);
}
